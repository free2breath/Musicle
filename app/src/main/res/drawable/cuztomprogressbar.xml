<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Define the background properties like color etc -->
    <item android:id="@android:id/background">
        <shape>
            <gradient
                android:startColor="#5b84b3"
                android:centerColor="#5b84b3"
                android:centerY="1.0"
                android:endColor="#5b84b3"
                android:angle="270"
                />
        </shape>
    </item>

    <!-- Define the progress properties like start color, end color etc -->
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <gradient
                    android:startColor="#2e455f"
                    android:centerColor="#2e455f"
                    android:centerY="1.0"
                    android:endColor="#2e455f"
                    android:angle="270"
                    />
            </shape>
        </clip>
    </item>
</layer-list>